apiVersion: v1
kind: Pod
metadata:
  name: app-pod
  labels:
    name: app-pod
    app: demo-app
spec:
  containers:
  - name: app
    image: dockersamples/examplevotingapp_vote
    ports:
      -containerPort: 80